default: &default
    adapter: postgresql
    encoding: unicode
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
    url: <%= ENV.fetch("DATABASE_URL") { "postgres://localhost/boss_lepan_academy_quests" } %>

development:
    <<: *default
    database: boss_lepan_academy_quests_development

test:
    <<: *default
    database: boss_lepan_academy_quests_test

production:
    primary: &primary_production
        <<: *default
        url: <%= ENV.fetch("DATABASE_URL") { "postgres://localhost/boss_lepan_academy_quests_prod" } %>
    cache:
        <<: *primary_production
    queue:
        <<: *primary_production
    cable:
        <<: *primary_production